'use client';

import { useEffect } from 'react';

export default function LoggerInitializer() {
   useEffect(() => {
      // Logger initialization logic if needed
   }, []);

   // Этот компонент ничего не рендерит
   return null;
}

